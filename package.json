{
  "name": "gumtree-diff",
  "displayName": "GumTree diff",
  "description": "a diff tool using the 'GumTree' algorithm",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gumtree-diff.helloWorld"
  ],
  "main": "./extension.js",
  "contributes": {
    "menus": {
      "explorer/context": [
        {
          "command": "gumtree-diff.source_select",
          "title": "GumTree diff: Select source file",
          "when": "listHasSelectionOrFocus"
        },
        {
          "command": "gumtree-diff.dest_select",
          "title": "GumTree diff: Select destination file",
          "when": "listHasSelectionOrFocus"
        }
      ]
    },
    "commands": [
      {
        "command": "gumtree-diff.source_select",
        "title": "GumTree diff: Select source file"
      },
      {
        "command": "gumtree-diff.dest_select",
        "title": "GumTree diff: Select destination file"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.75.0",
    "@types/glob": "^8.0.1",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "eslint": "^8.33.0",
    "glob": "^8.1.0",
    "mocha": "^10.1.0",
    "typescript": "^4.9.4",
    "@vscode/test-electron": "^2.2.2"
  }
}
